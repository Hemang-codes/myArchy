# Explicit defaults so they are easy to change. Adjust to taste.
input {
    kb_layout = us
    follow_mouse = 1

    touchpad {
        natural_scroll = false
        tap-to-click = true
        disable_while_typing = true
    }
}
