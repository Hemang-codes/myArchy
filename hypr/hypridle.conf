general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on   # avoids having to press a key twice to turn the display on
    ignore_dbus_inhibit = true                   # ignore idle-inhibit requests apps send over D-Bus
}

# 1. DIM DISPLAY (After 2.5 mins)
listener {
    timeout = 150                                
    on-timeout = brightnessctl -s set 10%       # dim backlight to 10% (a bare 10 would be a raw value)
    on-resume = brightnessctl -r                 # restore monitor backlight
}

# 2. LOCK SCREEN (After 5 mins)
listener {
    timeout = 300                                
    on-timeout = loginctl lock-session           # lock screen
}

# 3. TURN OFF SCREEN (After 5.5 mins)
listener {
    timeout = 330                                
    on-timeout = hyprctl dispatch dpms off       # screen off
    on-resume = hyprctl dispatch dpms on         # screen on when activity detected
}

# 4. SUSPEND/SHUTDOWN (Optional - After 30 mins)
listener {
    timeout = 1800                                
    on-timeout = systemctl suspend                # suspend pc
}

